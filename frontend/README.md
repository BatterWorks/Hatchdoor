# Hatchdoor Frontend

React, TypeScript, and Vite frontend for Hatchdoor.

Common commands:

```bash
npm ci
npm run dev
npm run build
npm run lint
npm run typecheck
npm test
```

In development, run the Rust backend on `127.0.0.1:42824`; Vite proxies `/api`
and `/health` to it.
