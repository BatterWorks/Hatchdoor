[
  {
    "lens": 0,
    "verdicts": [
      {
        "id": 0,
        "refuted": false,
        "reason": "Code matches: upsert_note_content (via upsert_note_if_changed) writes the notes/note_fts row before chunk_and_embed_note runs; on Err at line 63-67 only per_note_failures is incremented and a warn! is logged, the loop continues, and the same transaction still commits unconditionally at line 86 returning Ok(()). Change-detection in upsert_note_if_changed (188-201) compares only against the notes row's slug/snapshot/content_hash, so a note whose chunking failed will read as Unchanged next run and never retry chunk_and_embed_note."
      },
      {
        "id": 1,
        "refuted": false,
        "reason": "preserve_existing_vectors (542-562) keys reuse purely on content_hash via existing_chunk_hashes lookup with no embedder/model identity check anywhere in the reuse path (475-518), so the mechanism the finding describes is real in this source."
      },
      {
        "id": 2,
        "refuted": false,
        "reason": "The transaction is opened once at line 37-39 and not committed until line 86; the entire per-note loop at 56-69 (which calls chunk_and_embed_note, including embedder.embed() at line 500) executes inside that open transaction with no intermediate commit, exactly as claimed."
      },
      {
        "id": 3,
        "refuted": false,
        "reason": "create_schema's DDL is one execute_batch call (103-217) with no wrapping BEGIN/COMMIT, so each CREATE auto-commits independently under SQLite's implicit-transaction semantics, and the metadata schema_version INSERT is textually the final statement (209-211). existing_schema_version's None branch (74-79) genuinely returns a hard Err instructing the operator to delete the cache DB when metadata_exists but schema_version is missing, matching the claim."
      },
      {
        "id": 4,
        "refuted": false,
        "reason": "upsert_note_if_changed reads the file once at line 184 to derive content/hash for the notes row and note_fts, and chunk_and_embed_note independently re-reads the same path via fs::read_to_string at line 459 for chunking; there is no shared content or lock between the two reads, so the described TOCTOU divergence is a real gap in the code as written."
      }
    ]
  },
  {
    "lens": 1,
    "verdicts": [
      {
        "id": 0,
        "refuted": false,
        "reason": "Confirmed by code: upsert_note_content (called from upsert_note_if_changed, lines 176-213) commits the new notes/content_hash row via the same transaction, then chunk_and_embed_note (line 58) is called separately. Its Err path only increments per_note_failures and logs a warn (lines 63-67); the loop continues and tx.commit() at line 86 unconditionally persists everything, and the function returns Ok(()) with no signal to the caller. Change detection in upsert_note_if_changed (lines 188-201) only compares slug/snapshot/content_hash on the notes row -- it has no awareness of chunk/vector completeness -- so a note that got a new content_hash committed but failed chunking will be classified Unchanged on every future run and chunk_and_embed_note will never be retried. Under this lens (injecting a transient chunk/embed failure), the described corruption -- stale or missing chunk_vectors/chunk_fts while notes/note_fts is up to date, permanently -- does occur exactly as claimed."
      }
    ]
  },
  {
    "lens": 2,
    "verdicts": [
      {
        "id": 0,
        "refuted": false,
        "reason": "Code confirms the claim exactly: chunk_and_embed_note errors are caught at lines 63-67, only logged via warn!, loop continues, and tx.commit() at line 86 still runs unconditionally with function returning Ok(()). No transaction rollback, retry, or error-surfacing guards this path. Change-detection in upsert_note_if_changed (lines 188-201) only compares slug/snapshot/content_hash on the notes table, so a note whose chunk embed failed will be seen as Unchanged on next run and chunk_and_embed_note will never be retried. This is a genuine permanent-divergence defect from the atomicity/index-integrity lens; nothing in the cited file mitigates it."
      }
    ]
  }
]