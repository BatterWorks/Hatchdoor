[
  {
    "title": "Missing viewport-fit=cover defeats every env(safe-area-inset-*) rule on iOS",
    "severity": "high",
    "affectedClients": [
      "iOS Safari (WebKit)",
      "iOS installed PWA (WebKit)"
    ],
    "file": "frontend/index.html",
    "line": "14",
    "description": "The viewport meta is `content=\"width=device-width, initial-scale=1.0\"` with no `viewport-fit=cover`. Yet the CSS relies on env(safe-area-inset-*) in four files: topbar.css:4 (.hotbar height calc(3px + env(safe-area-inset-top))), responsive.css:10/53/87/95/144/148 (topbar top padding, actions-menu bottom offset, search panel, note pane, standalone padding), plus note-content.css and noteEnhancements.css.",
    "why": "WebKit only resolves env(safe-area-inset-*) to non-zero values when the page opts into the full display area via viewport-fit=cover. Without it, all four insets evaluate to 0 on notched/Dynamic-Island iPhones. Every safe-area guard silently collapses: the mobile actions menu, pinned with `bottom: calc(0.6rem + env(safe-area-inset-bottom))` (responsive.css:53), sits under the home indicator; the standalone PWA topbar padding (responsive.css:144) collapses so the header underlaps the status bar / Dynamic Island; the .hotbar accent renders as a bare 3px strip beneath the notch. Worst in the installed PWA because apple-mobile-web-app-capable=yes (index.html:7) makes it fullscreen. Chrome/Firefox/desktop unaffected.",
    "fixSketch": "Change the meta to content=\"width=device-width, initial-scale=1.0, viewport-fit=cover\" so the existing safe-area CSS actually takes effect.",
    "survives": true,
    "status": "verified",
    "refutes": 0,
    "votes": 3,
    "verdicts": [
      {
        "title": "Missing viewport-fit=cover defeats every env(safe-area-inset-*) rule on iOS",
        "refuted": false,
        "reason": "Confirmed at frontend/index.html:14 — viewport meta lacks viewport-fit=cover. This is a well-established WebKit behavior: env(safe-area-inset-*) resolves to 0 unless the page opts into the full display area with viewport-fit=cover, so all CSS relying on those insets (safe-area padding for topbar, actions menu, note pane, standalone PWA header) would collapse on notched/Dynamic Island iPhones, worst in the installed PWA due to apple-mobile-web-app-capable=yes at line 7. Real defect."
      },
      {
        "title": "Missing viewport-fit=cover defeats every env(safe-area-inset-*) rule on iOS",
        "refuted": false,
        "reason": "Verified: index.html:14 viewport meta is `width=device-width, initial-scale=1.0` with no viewport-fit=cover, and env(safe-area-inset-*) is genuinely used in topbar.css:4/7-8, responsive.css:10/53/86-87/95/144/148, note-content.css:165, and noteEnhancements.css:185/187/241/286 as claimed. Per WebKit spec, safe-area-inset-* env() variables resolve to 0 unless the page opts into viewport-fit=cover, so all these calc() expressions collapse to their base values on notched/Dynamic Island iPhones, producing exactly the described UI bugs (actions menu under home indicator, PWA header underlapping status bar/notch). apple-mobile-web-app-capable=yes at line 7 is also confirmed, making the installed PWA path fullscreen and thus most affected. This is a real, verifiable defect."
      },
      {
        "title": "Missing viewport-fit=cover defeats every env(safe-area-inset-*) rule on iOS",
        "refuted": false,
        "reason": "Confirmed at frontend/index.html:14 — the viewport meta tag is `width=device-width, initial-scale=1.0` with no `viewport-fit=cover`. This is a well-documented WebKit requirement: env(safe-area-inset-*) only resolves to non-zero values when the page opts into the full display area via viewport-fit=cover; otherwise all insets are 0. apple-mobile-web-app-capable=yes is also present at line 7, confirming this app supports installed PWA/standalone mode on iOS, which is exactly where notched/Dynamic-Island devices make this matter most. The finding correctly identifies a real, verifiable defect on a named target client (iOS Safari / installed iOS PWA) with no dependency on unverified downstream CSS specifics."
      }
    ]
  },
  {
    "title": "theme-color meta is a static light color and is never updated for dark mode",
    "severity": "medium",
    "affectedClients": [
      "iOS Safari (WebKit)",
      "iOS installed PWA (WebKit)",
      "Android Chrome"
    ],
    "file": "frontend/index.html",
    "line": "8",
    "description": "Only one `<meta name=\"theme-color\" content=\"#f4f1e8\" />` exists (light cream). No `media=\"(prefers-color-scheme: dark)\"` variant, and useTheme.ts (cycleTheme / the theme useEffect at useTheme.ts:14-21) only mutates document.documentElement.dataset.theme and localStorage, never the theme-color meta. A grep for theme-color across the frontend returns only this single static tag.",
    "why": "iOS Safari 15+, the iOS standalone PWA, and Android Chrome paint the browser chrome / status bar band from theme-color. In dark mode --bg is #0c0c0a (base.css:98 auto-dark, base.css:51 explicit dark) but the surrounding chrome stays #f4f1e8, producing a bright cream bar clashing with the dark UI. It never corrects when the user cycles to Dark or when the system is dark under theme=auto. Desktop browsers ignore theme-color. No apple-mobile-web-app-status-bar-style is set either, compounding the standalone case.",
    "fixSketch": "Add a second theme-color meta with media=\"(prefers-color-scheme: dark)\" for the theme=auto path, and in useTheme's effect update the meta content to match the resolved bg for manual light/dark overrides.",
    "survives": true,
    "status": "verified",
    "refutes": 0,
    "votes": 1,
    "verdicts": [
      {
        "title": "theme-color meta is a static light color and is never updated for dark mode",
        "refuted": false,
        "reason": "Confirmed only one static theme-color meta exists (index.html:8, #f4f1e8) with no media query variant, and grep across frontend/src finds zero other references to theme-color, meaning nothing updates it at runtime when the theme is cycled or system dark mode is active under theme=auto. This produces a light chrome/status-bar color clash in dark mode on iOS Safari/PWA and Android Chrome, a genuine defect."
      }
    ]
  },
  {
    "title": "Unguarded :hover on a mobile-only element leaves a sticky highlight on touch",
    "severity": "low",
    "affectedClients": [
      "iOS Safari (WebKit)",
      "Android Chrome"
    ],
    "file": "frontend/src/styles/topbar.css",
    "line": "267",
    "description": "`.topbar-mobile-path:hover { background: var(--paper-2) }` is not wrapped in @media (hover: hover), unlike the sibling .icon-button and .topbar-search-trigger hovers in the same file (topbar.css:116 and :160). This element renders ONLY on mobile: topbar.css:242 sets .topbar-mobile-meta { display: none } and responsive.css:44 flips it to display: block below 920px. The same unguarded pattern exists for explorer/search touch targets (layout-explorer.css:78 .note-link:hover, :164 folder summary:hover; search.css:111/132/159; ui-common.css:23).",
    "why": "On touch WebKit/Blink, tapping applies :hover and keeps it until the user taps elsewhere (no pointer to move away). The mobile path button opens the search overlay rather than navigating, so on return the --paper-2 background stays stuck. The author gated hover for the icon buttons but missed this touch-only element and the shared explorer/search styles. Purely cosmetic.",
    "fixSketch": "Wrap these :hover rules in @media (hover: hover) so they never fire on touch, matching the existing guarded rules in topbar.css.",
    "survives": true,
    "status": "unverified",
    "refutes": 0,
    "votes": 0,
    "verdicts": []
  }
]